<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="customButton" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">@color/myDarkGrey</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="customToolBar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/uniboRed</item>
        <item name="android:titleTextColor">@color/white</item>
    </style>

    <style name="background" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@layout/values</item>
        <item name="android:textColor">@color/uniboRed</item>
        <item name="android:textSize">32dp</item>
    </style>

    <style name="textStyle" parent="Widget.AppCompat.Toolbar">
        <item name="android:textColor">@color/uniboRed</item>
        <item name="android:textSize">32dp</item>
        <item name="android:layout_margin">10dp</item>
    </style>

</resources>
